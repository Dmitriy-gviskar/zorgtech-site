# AGENTS — zorgtech-site

Сайт эталона `zorgtech.com`. React + Vite + React Router + чистый CSS.
Без Tailwind / Next / TS без явной команды.

- Контент только с эталона. Не выдумывать. `zorgtech-hero` не трогать.
- Блог в данных есть — **в навигации не показывать**.
- Визуал: светлая студия, `#2aaadd`, Plus Jakarta Sans, pill CTA. Один язык.

Карта CSS — правило `styles` (когда открыты css). Картинки, bake, JSON — правило `zorgtech-plan`.
План этапов: `PLAN.md`.
